# 🚀 Полная инструкция: Запуск сервера 24/7

**Все готово!** У вас есть полностью рабочий сервер со всеми инструментами для продакшна.

## ⚡ Быстрая установка (1 команда)

Скопируйте проект на ваш сервер и выполните:

```bash
sudo bash install.sh
```

**Готово!** Сервер запущен и будет работать 24/7.

---

## 📋 Что вы получаете

✅ Сервер автоматически запускается при загрузке системы  
✅ Автоматический перезапуск при сбое  
✅ Полное логирование всех операций  
✅ Персистентное хранилище (сообщения + файлы)  
✅ Передача файлов через netcat  
✅ Удобные утилиты управления  

---

## 🎯 Ежедневные операции

### Проверить статус
```bash
sudo systemctl status serv_mess
```
или просто
```bash
./manage.sh status
```

### Смотреть логи в реальном времени
```bash
./manage.sh logs -f
```
или
```bash
tail -f /opt/serv_mess/logs/server.log
```

### Посмотреть статистику
```bash
./logs.sh stats
```

Вывод будет примерно так:
```
Total lines: 15342
File size: 1.2M

Logins: 245
Logouts: 242
Messages: 1205
Uploads: 89
Downloads: 156
Errors: 3
```

### Посмотреть только ошибки
```bash
./logs.sh errors
```

### Посмотреть активность пользователей
```bash
./logs.sh users
```

---

## 🔧 Управление сервером

### Перезагрузить (если вы обновили код)
```bash
./manage.sh restart
```

### Остановить
```bash
./manage.sh stop
```

### Запустить
```bash
./manage.sh start
```

### Получить информацию
```bash
./manage.sh info
```

Вывод:
```
=== Service Information ===
Service: serv_mess
Log file: /opt/serv_mess/logs/server.log

=== Service Status ===
● serv_mess.service - Message Server
   Loaded: loaded (/etc/systemd/system/serv_mess.service)
   Active: active (running) since Tue 2026-01-14 10:23:45 UTC

=== Port Status ===
LISTEN 192.168.1.100:7002

=== Log File Size ===
1.2M /opt/serv_mess/logs/server.log

=== Recent Activity ===
[2026-01-16 14:25:15] [INFO] User 'bob' posted message
```

---

## 📊 Мониторинг

### Посмотреть активные подключения
```bash
sudo netstat -tn | grep :7002 | wc -l
```

### Проверить процесс
```bash
ps aux | grep server.py
```

### Размер логов
```bash
du -h /opt/serv_mess/logs/server.log
```

### Размер хранилища
```bash
du -sh /opt/serv_mess/shared_files/
```

---

## 🔍 Анализ логов

### Все ошибки
```bash
grep ERROR /opt/serv_mess/logs/server.log
```

### Все логины
```bash
grep "logged in" /opt/serv_mess/logs/server.log
```

### Все сообщения
```bash
grep "posted message" /opt/serv_mess/logs/server.log
```

### Все файловые операции
```bash
grep -E "uploaded|downloaded" /opt/serv_mess/logs/server.log
```

### Логи за последний час
```bash
tail -f /opt/serv_mess/logs/server.log | grep "$(date '+%H')"
```

### Логи конкретного пользователя
```bash
grep "alice" /opt/serv_mess/logs/server.log
```

---

## 🛠️ Техническое обслуживание

### Очистить старые логи
```bash
sudo sh -c 'echo "" > /opt/serv_mess/logs/server.log'
```

### Архивировать логи перед очисткой
```bash
sudo tar czf /opt/serv_mess/logs/server.log.backup.$(date +%Y%m%d_%H%M%S).tar.gz \
  /opt/serv_mess/logs/server.log
sudo sh -c 'echo "" > /opt/serv_mess/logs/server.log'
```

### Перезагрузить сервер
```bash
sudo systemctl restart serv_mess
sleep 2
sudo systemctl status serv_mess
```

### Посмотреть все процессы Python
```bash
ps aux | grep python
```

---

## 🆘 Решение проблем

### Сервис не запускается
```bash
# Проверить статус
sudo systemctl status serv_mess -l

# Посмотреть журнал ошибок
sudo journalctl -u serv_mess -n 100 --no-pager

# Попробовать запустить вручную
cd /opt/serv_mess
python3 server.py
```

### Порт уже занят
```bash
# Найти процесс
sudo lsof -i :7002

# Убить процесс
sudo kill -9 <PID>

# Перезагрузить сервис
./manage.sh restart
```

### Нет прав доступа
```bash
# Переустановить права
sudo chown -R memo:memo /opt/serv_mess
sudo chmod -R 755 /opt/serv_mess
```

### Сервис падает постоянно
```bash
# Посмотреть логи
tail -50 /opt/serv_mess/logs/server.log

# Посмотреть журнал systemd
sudo journalctl -u serv_mess -f

# Проверить Python
python3 --version
```

---

## 📈 Производительность

### Максимум подключений одновременно
На одном потоке: ограничено только памятью и сокетами ОС (обычно 65k)

### Размер логов со временем
```
День 1:     ~50 KB (100 пользователей)
Неделя:     ~350 KB
Месяц:      ~1.5 MB
Год:        ~18 MB
```

### Размер сообщений
```
Среднее сообщение:     ~200 байт
Средний файл:          ~1 MB
```

---

## 🔐 Безопасность

### Проверить, от кого запущен сервер
```bash
ps aux | grep server.py
# Должен быть пользователь 'memo', НЕ root
```

### Проверить права доступа
```bash
ls -la /opt/serv_mess/logs/
ls -la /opt/serv_mess/shared_files/
# Должны быть владельцем 'memo'
```

### Ограничить доступ к логам
```bash
sudo chmod 750 /opt/serv_mess/logs/
sudo chmod 750 /opt/serv_mess/shared_files/
```

---

## 📝 Логи и отладка

### Все доступные уровни логирования
```
DEBUG     - Очень подробная информация
INFO      - Обычные операции
WARNING   - Предупреждения
ERROR     - Ошибки
CRITICAL  - Критические ошибки
```

### Посмотреть логи с временем
```bash
tail -f /opt/serv_mess/logs/server.log
```

Пример вывода:
```
[2026-01-16 14:23:45] [INFO] Server started on 0.0.0.0:7002
[2026-01-16 14:24:00] [INFO] Client connected: ('192.168.1.100', 12345)
[2026-01-16 14:24:05] [INFO] User 'alice' logged in from ('192.168.1.100', 12345)
[2026-01-16 14:25:15] [INFO] User 'alice' posted message
[2026-01-16 14:26:30] [INFO] User 'alice' uploaded file 'photo.jpg' (456789 bytes)
```

---

## 📱 Подключение клиентов

### Через netcat (telnet-style)
```bash
nc your-server.com 7002
login myname
help
post
Hello everyone!
END
```

### Через Python клиент (для файлов)
```bash
python3 client.py upload your-server.com:7002 myfile.txt
python3 client.py download your-server.com:7002 myfile.txt
```

---

## 🎓 Дополнительно

### Включить ротацию логов (Linux)
```bash
sudo cat > /etc/logrotate.d/serv_mess << 'EOF'
/opt/serv_mess/logs/server.log {
    daily
    rotate 30
    compress
    delaycompress
    notifempty
}
EOF
```

После этого логи будут автоматически:
- Сжиматься каждый день
- Удаляться через 30 дней
- Размер значительно сокращается

### Настройки firewall (UFW)
```bash
# Разрешить порт 7002
sudo ufw allow 7002

# Проверить
sudo ufw status
```

### Настройки firewall (iptables)
```bash
# Разрешить порт 7002
sudo iptables -A INPUT -p tcp --dport 7002 -j ACCEPT
```

---

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте логи: `./manage.sh logs`
2. Посмотрите статус: `./manage.sh status`
3. Обновите программу: скопируйте новые `server.py` и перезагрузите
4. Перезагрузитесь: `./manage.sh restart`

---

## ✅ Чек-лист

Перед использованием в продакшене:

- [ ] Выполнена установка: `sudo bash install.sh`
- [ ] Сервис запущен: `./manage.sh status` показывает "active"
- [ ] Логи доступны: `./manage.sh logs` выводит сообщения
- [ ] Клиент может подключиться: `nc localhost 7002` работает
- [ ] Порт открыт в firewall (если удаленный сервер)
- [ ] Достаточно свободного места на диске
- [ ] Установлена ротация логов (для длительной работы)

---

**Готово! Ваш сервер теперь работает 24/7 и готов к использованию.** 🎉
